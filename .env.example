# Only needed for the optional LLM router: `ask --llm`, `eval --llm`.
# The rule-based router is the default and needs no key.
# Nothing in this project reads an environment variable at import time.
OPENAI_API_KEY=
