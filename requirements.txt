# Three runtime dependencies. The original listed ten, of which
# `arango` was the wrong package name (the driver is `python-arango`),
# `cugraph` is not installable from PyPI at all, and `deepseek` was unused.
networkx>=3.2
beautifulsoup4>=4.12
flask>=3.0
# networkx does not declare numpy, but nx.pagerank imports it -- and pagerank
# is one of the seven allowlisted algorithms the router can execute. Missing it
# only showed up in CI, because a clean install is the one place nothing else
# has already pulled numpy in.
numpy>=1.26

# Optional: only needed for `ask --llm` / `eval --llm`.
# openai>=1.0
